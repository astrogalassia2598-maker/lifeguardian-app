name: lifeguardian_app
description: LifeGuardian - gestione scadenze automatica
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  device_calendar: ^4.3.0
  file_picker: ^8.0.0
  image_picker: ^1.0.0
  google_mlkit_text_recognition: ^0.13.0
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
